buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'io.ratpack:ratpack-gradle:1.3.3'
  }
}

apply plugin: 'io.ratpack.ratpack-groovy'

repositories {
  jcenter()
  flatDir {
    dirs 'libs'
  }
}

dependencies {
  compile name: 'dbconfig-0.0.1'
}
